# Инструкция для первого использования Git и GitHub

## Шаг 1: Настройка Git (обязательно!)

Git требует знать, кто вы, перед первым коммитом. Выполните эти команды:

### Вариант 1: Глобальная настройка (для всех проектов)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш_email@example.com"
```

**Пример:**
```bash
git config --global user.name "Иван Иванов"
git config --global user.email "ivan@example.com"
```

### Вариант 2: Настройка только для этого проекта

Если хотите использовать разные данные только для этого проекта:

```bash
git config user.name "Ваше Имя"
git config user.email "ваш_email@example.com"
```

**Важно:** 
- Используйте реальный email, который привязан к вашему GitHub аккаунту (если есть)
- Или любой email, если аккаунта еще нет

## Шаг 2: Проверка настройки

Убедитесь, что настройки применены:

```bash
git config --global user.name
git config --global user.email
```

Должны отобразиться ваши имя и email.

## Шаг 3: Продолжение работы с проектом

После настройки Git, выполните:

### 3.1. Проверка статуса

```bash
git status
```

Покажет, какие файлы готовы к коммиту.

### 3.2. Добавление файлов

```bash
git add .
```

Эта команда добавляет все файлы в "staging area" (область подготовки).

### 3.3. Создание первого коммита

```bash
git commit -m "Initial commit: Система бронирования ресторана"
```

Теперь коммит должен пройти успешно!

### 3.4. Проверка коммита

```bash
git log
```

Покажет историю коммитов (должен быть ваш первый коммит).

## Шаг 4: Создание репозитория на GitHub

### 4.1. Создайте аккаунт на GitHub (если еще нет)

1. Перейдите на https://github.com
2. Нажмите "Sign up"
3. Заполните форму регистрации
4. Подтвердите email

### 4.2. Создайте новый репозиторий

1. Войдите в GitHub
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните форму:
   - **Repository name:** `booking-system` (или другое название)
   - **Description:** `Система управления бронированием столов в ресторане`
   - **Public** или **Private** (выберите по желанию)
   - **НЕ** ставьте галочки на:
     - ❌ Add a README file (у нас уже есть)
     - ❌ Add .gitignore (у нас уже есть)
     - ❌ Choose a license (у нас уже есть)
5. Нажмите **"Create repository"**

### 4.3. GitHub покажет инструкции

После создания репозитория GitHub покажет страницу с инструкциями. 
**НЕ следуйте инструкциям "create a new repository"** - у нас уже есть локальный репозиторий!

Вместо этого используйте раздел **"...or push an existing repository from the command line"**

## Шаг 5: Подключение к GitHub

### 5.1. Добавьте удаленный репозиторий

```bash
git remote add origin https://github.com/YOUR_USERNAME/booking-system.git
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

Например, если ваш username `ivan123`, команда будет:
```bash
git remote add origin https://github.com/ivan123/booking-system.git
```

### 5.2. Переименуйте ветку в main (если нужно)

```bash
git branch -M main
```

### 5.3. Отправьте код на GitHub

```bash
git push -u origin main
```

**При первом push Git попросит авторизацию:**

#### Вариант A: Personal Access Token (рекомендуется)

1. GitHub попросит username и password
2. **Username** - ваш GitHub username
3. **Password** - НЕ ваш обычный пароль! Нужен Personal Access Token

**Как создать Token:**
1. GitHub → Settings (настройки профиля)
2. Внизу слева: **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token (classic)**
5. Название: `booking-system`
6. Выберите срок действия (например, 90 days)
7. Отметьте галочку **`repo`** (полный доступ к репозиториям)
8. Нажмите **Generate token**
9. **Скопируйте токен** (он показывается только один раз!)
10. Используйте этот токен как пароль при `git push`

#### Вариант B: GitHub CLI (альтернатива)

Можно установить GitHub CLI и использовать его для авторизации:
```bash
gh auth login
```

## Шаг 6: Проверка

После успешного `git push`:

1. Обновите страницу репозитория на GitHub
2. Вы должны увидеть все ваши файлы
3. README.md должен отображаться на главной странице

## Частые проблемы и решения

### Проблема: "fatal: не удалось выполнить автоопределение адреса электронной почты"

**Решение:** Выполните шаг 1 (настройка Git)

### Проблема: "remote origin already exists"

**Решение:** Удалите старый remote и добавьте заново:
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/booking-system.git
```

### Проблема: "Permission denied" при push

**Решение:** 
- Проверьте правильность username в URL
- Используйте Personal Access Token вместо пароля
- Убедитесь, что токен имеет права `repo`

### Проблема: "Updates were rejected"

**Решение:** Если на GitHub уже есть файлы (например, README), сначала получите их:
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

## Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть настройки
git config --list

# Посмотреть удаленные репозитории
git remote -v

# Отменить изменения в файле (до add)
git checkout -- filename

# Отменить add (убрать из staging)
git reset HEAD filename
```

## Дальнейшая работа

После первого push, для обновления кода на GitHub:

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

**Удачи с публикацией проекта!** 🚀
