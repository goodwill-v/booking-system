# Система бронирования ресторана 🍽️

Система управления бронированием столов в ресторане с графическим интерфейсом. Позволяет управлять пользователями, столами и бронированиями с автоматической проверкой доступности и предотвращением конфликтов по времени.

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-12+-blue.svg)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Настройка базы данных](#настройка-базы-данных)
- [Запуск приложения](#запуск-приложения)
- [Использование графического интерфейса](#использование-графического-интерфейса)
- [Структура проекта](#структура-проекта)
- [API и функции](#api-и-функции)

## 🔧 Требования

- Python 3.8 или выше
- PostgreSQL 12 или выше
- pip (менеджер пакетов Python)

## 📦 Установка

1. **Клонируйте или скачайте проект**

2. **Создайте виртуальное окружение (рекомендуется):**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   - На Linux/Mac:
     ```bash
     source venv/bin/activate
     ```
   - На Windows:
     ```bash
     venv\Scripts\activate
     ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

## 🗄️ Настройка базы данных

1. **Создайте базу данных PostgreSQL:**
   ```sql
   CREATE DATABASE booking_system;
   ```

2. **Создайте файл `.env` в корне проекта:**
   ```bash
   cp .env.example .env
   ```
   Затем откройте `.env` и заполните реальными параметрами подключения к PostgreSQL:
   ```env
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=booking_system
   DB_USER=postgres
   DB_PASSWORD=your_password
   ```
   Замените значения на ваши параметры подключения к PostgreSQL.

3. **Инициализируйте таблицы базы данных:**
   ```bash
   python backend.py
   ```
   Эта команда создаст все необходимые таблицы:
   - `users` - пользователи системы
   - `restaurant_tables` - столы ресторана
   - `bookings` - бронирования

## 🚀 Запуск приложения

Запустите графический интерфейс:
```bash
python gui.py
```

Или через main.py:
```bash
python main.py
```

После запуска откроется окно приложения с вкладками для управления различными компонентами системы.

## 🖥️ Использование графического интерфейса

### Вкладка "Пользователи"

Управление пользователями системы.

#### Создание пользователя:
1. Заполните поля формы:
   - **Email** - уникальный email пользователя (обязательно)
   - **Пароль** - пароль (будет сохранен как хэш)
   - **Полное имя** - имя пользователя (обязательно)
   - **Телефон** - номер телефона (опционально)
   - **Роль** - роль пользователя (`client` или `admin`)
   - **Активен** - флаг активности пользователя
2. Нажмите кнопку **"Создать"**

#### Просмотр пользователей:
- Список всех пользователей отображается в таблице внизу вкладки
- Двойной клик по пользователю загружает его данные в форму для редактирования

#### Обновление пользователя:
1. Дважды кликните по пользователю в списке (или введите ID вручную)
2. Измените нужные поля
3. Нажмите кнопку **"Обновить"**

#### Удаление пользователя:
1. Выберите пользователя (двойной клик или введите ID)
2. Нажмите кнопку **"Удалить"**
3. Подтвердите удаление

### Вкладка "Столы"

Управление столами ресторана.

#### Создание стола:
1. Заполните поля формы:
   - **Номер стола** - уникальный номер стола (обязательно)
   - **Вместимость** - количество мест (обязательно)
   - **Тип стола** - тип (`standard`, `vip`, `window`, `outdoor` и т.д.)
   - **Статус** - текущий статус (`available`, `reserved`, `occupied`)
   - **Расположение** - расположение стола (опционально)
   - **Описание** - дополнительное описание (опционально)
   - **Активен** - флаг активности стола
2. Нажмите кнопку **"Создать"**

#### Просмотр столов:
- Список всех столов отображается в таблице
- Двойной клик по столу загружает его данные в форму

#### Обновление и удаление:
- Аналогично работе с пользователями

### Вкладка "Бронирования"

Управление бронированиями столов.

#### Создание бронирования:
1. Заполните поля формы:
   - **ID пользователя** - ID пользователя, который делает бронирование (обязательно)
     - Используйте кнопку **"..."** для выбора пользователя из списка
   - **ID стола** - ID стола для бронирования (обязательно)
     - Используйте кнопку **"..."** для выбора стола из списка
   - **Дата бронирования** - дата в формате `YYYY-MM-DD` (обязательно)
   - **Время начала** - время начала в формате `HH:MM` (обязательно)
   - **Время окончания** - время окончания в формате `HH:MM` (обязательно)
   - **Количество гостей** - количество гостей (обязательно)
   - **Статус** - статус бронирования (`pending`, `confirmed`, `cancelled`, `completed`)
   - **Заметки** - дополнительные заметки (опционально)
2. Нажмите кнопку **"Создать"**

**Важно:** Система автоматически проверяет доступность стола перед созданием бронирования. Если стол уже забронирован на указанное время или время пересекается с существующим бронированием, бронирование не будет создано, и вы получите сообщение об ошибке.

#### Просмотр бронирований:
- Список всех бронирований отображается в таблице
- Двойной клик по бронированию загружает его данные в форму

#### Обновление бронирования:
1. Выберите бронирование (двойной клик или введите ID)
2. Измените нужные поля
3. Нажмите кнопку **"Обновить"**

**Примечание:** При обновлении также проверяется доступность стола на новое время (текущее бронирование исключается из проверки).

#### Удаление бронирования:
- Аналогично удалению пользователей

### Вкладка "Проверка доступности"

Проверка доступности стола на определенное время без создания бронирования.

#### Использование:
1. Заполните поля:
   - **ID стола** - ID стола для проверки (используйте кнопку **"..."** для выбора)
   - **Дата бронирования** - дата в формате `YYYY-MM-DD`
   - **Время начала** - время начала в формате `HH:MM`
   - **Время окончания** - время окончания в формате `HH:MM`
   - **Исключить бронирование ID** - опционально, ID бронирования для исключения из проверки (полезно при обновлении)
2. Нажмите кнопку **"Проверить доступность"**

#### Результаты проверки:
В области результатов отображается:
- ✅ **СТОЛ ДОСТУПЕН** - стол свободен на указанное время
- ❌ **СТОЛ НЕДОСТУПЕН** - стол занят или время пересекается с существующим бронированием
  - При конфликте отображается информация о конфликтующем бронировании (ID, пользователь, время)

## 📁 Структура проекта

```
Booking/
├── models/                 # Модели данных
│   ├── users.py           # Модель пользователя
│   ├── tables.py          # Модель стола
│   └── booking.py         # Модель бронирования
├── backend.py             # Backend с CRUD операциями
├── postgres_driver.py     # Драйвер для работы с PostgreSQL
├── gui.py                 # Графический интерфейс
├── main.py                # Точка входа приложения
├── requirements.txt       # Зависимости проекта
├── .env                   # Переменные окружения (создайте вручную)
└── README.md              # Документация
```

## 🔌 API и функции

### Backend функции

#### Пользователи (Users):
- `create_user(user, driver)` - создание пользователя
- `read_user(user_id, driver)` - чтение пользователя по ID
- `read_users(where=None, driver=None)` - чтение списка пользователей с фильтрацией
- `update_user(user_id, user, driver)` - обновление пользователя
- `delete_user(user_id, driver)` - удаление пользователя
- `get_all_users(driver=None)` - получение всех пользователей

#### Столы (Tables):
- `create_table(table, driver)` - создание стола
- `read_table(table_id, driver)` - чтение стола по ID
- `read_tables(where=None, driver=None)` - чтение списка столов с фильтрацией
- `update_table(table_id, table, driver)` - обновление стола
- `delete_table(table_id, driver)` - удаление стола
- `get_all_tables(driver=None)` - получение всех столов

#### Бронирования (Bookings):
- `create_booking(booking, driver)` - создание бронирования (с проверкой доступности)
- `read_booking(booking_id, driver)` - чтение бронирования по ID
- `read_bookings(where=None, driver=None)` - чтение списка бронирований с фильтрацией
- `update_booking(booking_id, booking, driver)` - обновление бронирования (с проверкой доступности)
- `delete_booking(booking_id, driver)` - удаление бронирования
- `get_all_bookings(driver=None)` - получение всех бронирований
- `check_table_availability(table_id, booking_date, booking_time, booking_end_time, driver, exclude_booking_id=None)` - проверка доступности стола

#### Инициализация:
- `create_all_tables(driver=None)` - создание всех таблиц в базе данных

## 🔒 Логика проверки доступности

Система автоматически предотвращает конфликты бронирований:

- **Проверка пересечений:** Система проверяет, не пересекается ли новое бронирование с существующими активными бронированиями (статусы `pending` или `confirmed`)
- **Запрет двойного бронирования:** Один стол не может быть забронирован двумя разными пользователями на пересекающиеся периоды времени
- **Примеры:**
  - Если стол забронирован с 19:00 до 21:00, нельзя забронировать:
    - 20:00-22:00 (пересекается)
    - 18:00-20:00 (пересекается)
    - 18:30-20:30 (пересекается)
  - Можно забронировать:
    - 17:00-19:00 (не пересекается)
    - 21:00-23:00 (не пересекается)

## ⚠️ Важные замечания

1. **База данных:** Убедитесь, что PostgreSQL запущен и доступен перед запуском приложения
2. **Переменные окружения:** Файл `.env` должен содержать корректные параметры подключения
3. **Инициализация:** При первом запуске обязательно выполните `python backend.py` для создания таблиц
4. **Время бронирования:** Время окончания должно быть позже времени начала
5. **Уникальность:** Email пользователя и номер стола должны быть уникальными

## 🐛 Решение проблем

### Ошибка подключения к базе данных
- Проверьте, запущен ли PostgreSQL
- Убедитесь, что параметры в `.env` корректны
- Проверьте права доступа пользователя к базе данных

### Ошибка при создании таблиц
- Убедитесь, что база данных существует
- Проверьте права пользователя на создание таблиц

### Ошибка при создании бронирования
- Проверьте, что стол существует и активен
- Убедитесь, что время окончания позже времени начала
- Проверьте, что стол не занят на указанное время

## 📝 Примеры использования

### Пример создания пользователя через код:
```python
from postgres_driver import PostgreSQLDriver
from backend import create_user
from models.users import User

driver = PostgreSQLDriver()
driver.connect()

user = User(
    email="user@example.com",
    password_hash="hashed_password",
    full_name="Иван Иванов",
    phone="+79001234567",
    role="client"
)

user_id = create_user(user, driver)
print(f"Создан пользователь с ID: {user_id}")

driver.disconnect()
```

### Пример проверки доступности стола:
```python
from datetime import date, time
from backend import check_table_availability

result = check_table_availability(
    table_id=1,
    booking_date=date(2024, 12, 25),
    booking_time=time(19, 0),
    booking_end_time=time(21, 0),
    driver=driver
)

if result['available']:
    print("Стол доступен!")
else:
    print(f"Стол занят. Конфликт: {result['conflicting_booking']}")
```

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🤝 Вклад в проект

Мы приветствуем любые вклады! Пожалуйста, прочитайте [CONTRIBUTING.md](CONTRIBUTING.md) для получения информации о процессе внесения изменений.

## 📝 Быстрая настройка

Для быстрого старта см. [SETUP.md](SETUP.md)

## 👤 Автор

Система бронирования ресторана - учебный проект для работы с базами данных.

## ⭐ Звезды

Если проект оказался полезным, поставьте звезду ⭐ на GitHub!

---

**Приятного использования!** 🍽️
