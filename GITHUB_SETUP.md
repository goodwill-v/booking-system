# Инструкция по публикации проекта на GitHub

## Подготовка проекта

Проект уже подготовлен для публикации на GitHub. Все необходимые файлы созданы:

- ✅ `.gitignore` - исключает ненужные файлы (venv, __pycache__, .env)
- ✅ `.env.example` - пример файла с переменными окружения
- ✅ `README.md` - подробная документация
- ✅ `LICENSE` - лицензия MIT
- ✅ `CONTRIBUTING.md` - руководство для контрибьюторов
- ✅ `SETUP.md` - инструкция по быстрой настройке
- ✅ `.github/ISSUE_TEMPLATE/` - шаблоны для issues

## Шаги для публикации

### 1. Инициализация Git репозитория (если еще не сделано)

```bash
cd /path/to/Booking
git init
```

### 2. Добавление файлов

```bash
git add .
```

**Важно:** Убедитесь, что файл `.env` НЕ добавлен в репозиторий (он должен быть в .gitignore)

### 3. Первый коммит

```bash
git commit -m "Initial commit: Система бронирования ресторана"
```

### 4. Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Нажмите кнопку "New repository" (или "+" → "New repository")
3. Заполните:
   - **Repository name:** `booking-system` (или другое название)
   - **Description:** `Система управления бронированием столов в ресторане с графическим интерфейсом`
   - **Visibility:** Public (или Private, если хотите)
   - **НЕ** добавляйте README, .gitignore или LICENSE (они уже есть в проекте)
4. Нажмите "Create repository"

### 5. Подключение локального репозитория к GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/booking-system.git
```

Замените `YOUR_USERNAME` на ваш GitHub username.

### 6. Отправка кода на GitHub

```bash
git branch -M main
git push -u origin main
```

## Проверка

После публикации проверьте:

1. ✅ Все файлы загружены (кроме тех, что в .gitignore)
2. ✅ README.md отображается на главной странице
3. ✅ .env.example виден в репозитории
4. ✅ .env НЕ виден в репозитории (безопасность)
5. ✅ LICENSE отображается
6. ✅ Структура проекта корректна

## Дополнительные настройки на GitHub

### Добавление тегов (опционально)

```bash
git tag -a v1.0.0 -m "Первая версия системы бронирования"
git push origin v1.0.0
```

### Настройка описания репозитория

На странице репозитория нажмите ⚙️ (Settings) → в разделе "Features" можно включить:
- Issues
- Projects
- Wiki (если нужно)

### Добавление тегов (Topics)

На странице репозитория нажмите ⚙️ (Settings) → внизу страницы добавьте теги:
- `python`
- `postgresql`
- `tkinter`
- `booking-system`
- `restaurant-management`

## Обновление проекта

После внесения изменений:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте файл `.env` с реальными паролями
- Используйте `.env.example` как шаблон
- Если случайно закоммитили `.env`, немедленно смените пароли в базе данных

✅ **Рекомендации:**
- Регулярно делайте коммиты с понятными сообщениями
- Используйте ветки для новых функций
- Обновляйте README при значительных изменениях

## Полезные ссылки

- [GitHub Guides](https://guides.github.com/)
- [Git Documentation](https://git-scm.com/doc)
- [Markdown Guide](https://www.markdownguide.org/)

---

**Готово!** Ваш проект теперь на GitHub! 🎉
